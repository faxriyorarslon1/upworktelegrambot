# 4) Write a query in postgresql to retrieve all the rows from a table named "customers" where the "city" column value is 'London'.

# SELECT * FROM customers WHERE city = 'London';

# 5)Write a query in postgresql to retrieve the total number of orders placed by a customer with a given "customer_id" value '1234' from a table named "orders".
# SELECT COUNT(*) FROM orders WHERE customer_id = '1234';
